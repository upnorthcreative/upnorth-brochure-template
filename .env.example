# ─────────────────────────────────────────────────────────────────────────────
# Environment variables for the Up North Creative brochure template
#
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# Never commit .env.local — it is already in .gitignore
# ─────────────────────────────────────────────────────────────────────────────

# ── Site ─────────────────────────────────────────────────────────────────────
# Public URL of the deployed site (no trailing slash).
# Used for sitemaps, og:url, and JSON-LD structured data.
NEXT_PUBLIC_SITE_URL=https://yoursite.com

# ── Resend (contact form email delivery) ─────────────────────────────────────
# Get your API key at resend.com → API Keys
# The sending domain (contactForm.fromEmail in lib/content.ts) must be
# verified in your Resend account before emails will deliver.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# ── Cloudflare Turnstile (spam protection) ───────────────────────────────────
# Get keys at dash.cloudflare.com → Turnstile → Add Site
# Use the test keys locally:
#   Site key:   1x00000000000000000000AA
#   Secret key: 1x0000000000000000000000000000000AA
# Leave blank to disable Turnstile (form still works — honeypot + rate limiting remain active).
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
TURNSTILE_SECRET_KEY=

# ── Google Reviews (Places API) ──────────────────────────────────────────────
# Powers the live Reviews section. Both values are read SERVER-SIDE only and are
# never exposed to the browser (note: no NEXT_PUBLIC_ prefix).
#
# If either value is missing, the Reviews section hides itself automatically —
# there is no hardcoded fallback content, so the site never breaks or shows
# fake reviews. The live Google rating + review count also feed the hero/about
# stat grids and hide gracefully when unavailable.
#
# Setup:
#   1. Google Cloud Console → enable the "Places API (New)".
#   2. APIs & Services → Credentials → create an API key.
#   3. Restrict the key to the Places API (and ideally to your server/IP).
#   4. Find your Place ID: https://developers.google.com/maps/documentation/places/web-service/place-id
#
# Provider switch (Places now / Business Profile later) lives in
# lib/content.ts → siteConfig.reviews.provider.
GOOGLE_PLACES_API_KEY=
GOOGLE_PLACE_ID=

# ── Analytics ─────────────────────────────────────────────────────────────────
# Analytics IDs (GA4, GTM, Meta Pixel, Microsoft Clarity) are set directly in
# lib/content.ts under siteConfig.analytics — no environment variable needed.
